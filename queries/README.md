# KQL Queries

This directory contains all KQL queries used during the investigation.

Queries are grouped by purpose, including:
- Initial access detection
- Lateral movement
- Credential access
- Data staging and exfiltration
- Persistence mechanisms

Each query directly supports findings documented in the report.

